# 車中泊さがし — 仕様書

現地その場探しに特化した、個人用の車中泊スポット検索 Web アプリの仕様書。
Claude Code でこのアプリを再構築・保守・拡張するための資料。

- 現行実装: `index.html`（単一ファイル）
- 初期データ: `data/spots.seed.json`（73 件）
- 公開URL: <https://ask-holdings.github.io/shachuhaku-spots/>
- 最終更新: 2026-08-12

---

## 1. 目的とユースケース

週末に釣りをして、**その近くで夜に車中泊できる場所を、その場ですぐ探す**ためのアプリ。

- 主な場面: 夕マヅメに竿を片付けたあと、暗くなる前後に「今この辺で泊まれる所」を探す。
- 到着は **夕方〜夜**。したがって「景色が良い」より「夜も使えるトイレ・静けさ・安心して眠れるか」を優先する。
- 対象エリア: 長野を中心に、新潟・富山・山梨・静岡・岐阜・群馬など隣接県。
- 利用者: 開発者本人（ソロ／少人数）。ログインや共有は不要。
- 電波: たまに弱いが頻度は低い。フルオフラインは不要。ただし **スポット情報だけは通信なしでも見られる**ことを重視（データはアプリに同梱）。

### 設計の中心思想（変更しないこと）

1. **距離が主役。** 開いたら現在地中心で「近い順」。各行は距離を大きく先頭に表示。地名検索は不要。
2. **夜向けの情報設計。** 一覧は即決材料だけ（距離・トイレ・24時間・無料・温泉・静けさ）。詳細で夜の安心材料とメモ。
3. **正直さ（可否の根拠を隠さない）。** 「本来車中泊用でない／要予約」の場所は必ず区別して表示する。OSM ライブ検索の結果は「未確認の候補」として明示する。
4. **軽い保険としてのオフライン。** 同梱データは通信なしで表示可。地図タイルのオフライン化は当面やらない（割り切り）。

---

## 2. 技術スタック / 制約

| 項目 | 内容 |
|---|---|
| 形態 | 単一の静的 HTML ファイル（`index.html`）。ビルド不要。 |
| 地図 | Leaflet 1.9.4（CDN） + OpenStreetMap タイル |
| フォント | Zen Kaku Gothic New（日本語） / Space Grotesk（数字）— Google Fonts、システムフォントにフォールバック |
| 保存 | ブラウザの localStorage（ユーザー追加スポット＋フィルター等の状態） |
| ライブ検索 | Overpass API（OpenStreetMap のクエリ）— 要通信・3系統フォールバック |
| ナビ連携 | Google マップの URL スキーム（経路案内・場所表示） |
| バックエンド | なし（サーバーレス・DBなし） |
| 認証 | なし |
| デプロイ | GitHub Pages（`ask-holdings/shachuhaku-spots`、main / root）。`git push` で自動反映。 |

### なぜ単一ファイルか

静的ホスティングに 1 ファイル置くだけで公開でき、データ同梱でオフラインでも一覧が出るため。
Claude Code で扱う際も **単一ファイルを維持することを推奨**。データは `index.html` 冒頭の `const SPOTS = [...]` にまとめてあるので、そこだけ差し替えれば更新できる。
（将来データが数百件規模になったら `data/spots.seed.json` を fetch する案もあるが、その場合オフライン表示のために Service Worker でのキャッシュが前提になる。=> 第 9 章ロードマップ参照）

---

## 3. データモデル

スポット 1 件のオブジェクト:

```js
{
  n:     string,   // 名前（必須）
  cat:   string,   // 種類（例: "道の駅", "RVパーク", "キャンプ場", "漁港・海沿い"）
  area:  string,   // 市町村（任意・空文字可）
  chk:   boolean,  // 要確認フラグ（本来車中泊用でない／要予約）
  lat:   number,   // 緯度
  lng:   number,   // 経度
  q:     number,   // 静けさ 1=騒がしめ 2=ふつう 3=静か（0=不明）
  wc:    boolean,  // トイレ
  h24:   boolean,  // 24時間 出入り可
  free:  boolean,  // 駐車 無料
  onsen: boolean,  // 温泉が近い
  m:     string,   // メモ
  t:     number,   // ティア（下表）
  id?:   string    // t===3（自分の追加）のみ。ユニークID 'u'+Date.now().toString(36)
}
```

### ティア（`t`）— スポットの出所と信頼度

| t | 名称 | 件数 | データの質 | ピン色 | 一覧タグ |
|---|---|---|---|---|---|
| 0 | 確認済み | 45 | 手作業でキュレーション、設備まで整備 | 種類ごと（下記） | なし |
| 1 | 自動追加候補 | 28 | 座標は実データ、設備は口コミ推定 | 種類ごと | 「候補」 |
| 2 | OSM ライブ | 実行時 | 名前＋座標のみ・未確認 | 紫 | 「OSM候補」 |
| 3 | 自分の追加 | ユーザー | ユーザー入力・localStorage 保存 | 緑 | 「自分の」 |

- **t=0, 1** は同梱データ（`SPOTS` 配列 / `data/spots.seed.json`）。
- **t=2** はライブ検索の結果。localStorage（`carcamp_live_v1`）に24時間キャッシュされ、再起動時に復元される。設備データは持たない。
- **t=3** は localStorage（キー `carcamp_myspots_v1`）に保存。編集・削除可能。

---

## 4. 画面と機能仕様

### 4.1 レイアウト（上から）

1. **ヘッダー**: アプリ名＋ステータス（「現在地から近い順・追尾中 / GPS ±Xm」など）。
2. **地図**（高さ 54dvh ⇔ 74dvh を右上ボタンでトグル・状態保存）: 全スポットのピン、現在地/基準点マーカー。
   地図上のオーバーレイ: 上部=**フィルターチップ**（トイレ/24時間/無料/温泉近く/静か。複数選択はAND・地図のピンも連動・状態保存）、
   右上=拡大トグル、左下=凡例、下中央=**「このあたりを検索」**（ライブ検索）。
3. **リスト**: 近い順のカード。**距離帯見出し**（~1km/~5km/~15km/~50km/50km~）で区切る。
   末尾に「自分の追加 N件・書き出し/読み込み」フッター。
4. **FAB（右下）**: 緑「＋」＝追加、下に「◎」＝現在地（追尾トグル）。
5. **ボトムシート**（3種）: 詳細 / 追加フォーム / データ入出力。下スワイプ・Escape・背景タップで閉じる。

### 4.2 位置の取得と距離

- 起動時に `navigator.geolocation.getCurrentPosition` で現在地取得 → 地図センター＆「近い順」。
- 「◎」FAB は **追尾モードのトグル**（`watchPosition`）。追尾中は精度円を表示し、30m 以上移動で自動再ソート。
  一時的な取得失敗（トンネル等, code 2/3）では追尾を維持し、位置情報の拒否（code 1）のみ停止する。
- 取得失敗・拒否・圏外時は「地図をタップして基準点を置く」フォールバック。タップ地点を基準に近い順を計算（追尾は自動停止）。
- 基準点・フィルター等は localStorage（`carcamp_state_v1`）に保存し、次回起動時に復元（「前回の基準点から近い順」）。
- 距離は **ハバースイン公式**（`dist()`）。1km 未満は m、10km 未満は 0.1km 単位、以上は 1km 単位。
  **8 方位**（`bearingTxt()`）＋方位角で回転する矢印も表示。
- 最寄りの 1 件はピンが **琥珀色に点滅**（`.pin.near`）。t=2 は対象外。

### 4.3 一覧カード

- 左に距離（大きい数字＋単位＋方角）、右に名前・種類・エリア・タグ・設備アイコン・静けさ（3点）。
- t=2（OSM）のカードは設備を出さず「地図データから自動取得。設備・車中泊可否は未確認」と明記。

### 4.4 詳細ボトムシート

- 距離・方角、種類・エリア、注記、設備（ラベル付き）、静けさ、メモ。表示中は該当ピンを拡大強調し地図をパン。
- 注記の出し分け:
  - `chk===true` → 警告（本来車中泊用でない／要予約。現地確認を）
  - `t===3` → 「自分で追加。編集・削除できます」
  - `t===1` → 「自動追加候補。設備は口コミ推定」
  - `t===2` → 「OSM から自動取得。未確認」
- アクション:
  - **経路案内**: `https://www.google.com/maps/dir/?api=1&destination=<lat>,<lng>`（座標で確実にナビ）
  - **地図で見る**: `https://www.google.com/maps/search/?api=1&query=<名前+エリア>`（名前がなければ座標）。
    → Google マップで場所を開き、そこから **写真・ストリートビュー**で行く前に雰囲気を確認できる。これが「地図で見る」の目的。
  - t=3 のみ **編集 / 削除** を追加表示。

### 4.5 フィルター

- 5 つの真偽フィルターの AND（静か = `q>=3`）。地図のピン表示も連動する。
- t=2（設備不明）は、いずれかのフィルターが ON のとき一覧・地図から除外し、
  「OSM候補 N件は設備が未確認のため、フィルター中は表示していません」の notice を出す。

### 4.6 ライブ検索（Overpass / OpenStreetMap）

- 目的: 同梱していない道の駅・RVパーク・キャンプ場等を、現在地周辺からその場で拾う。
- 起動: 地図下中央の「このあたりを検索」。基準点が無ければ地図中心を基準点にする。
- 半径: **表示中の地図範囲から自動決定**（3〜30km）。
- エンドポイント（POST, `data=` にクエリ）: 以下の順でフォールバック。
  1. `https://overpass-api.de/api/interpreter`（タイムアウト12s）
  2. `https://overpass.kumi.systems/api/interpreter`（20s）
  3. `https://overpass.private.coffee/api/interpreter`（20s）
- クエリは **2 本に分離**して並行実行する（重要）:
  - **タグ検索（本命・これが失敗したら初めてエラー扱い）**
    ```
    [out:json][timeout:25];
    (
      nwr["tourism"="caravan_site"](around:R,la,ln);
      nwr["tourism"="camp_site"](around:R,la,ln);
      nwr["highway"="rest_area"](around:R,la,ln);
      nwr["highway"="services"](around:R,la,ln);
    );
    out center 120;
    ```
  - **名前の正規表現検索（Overpass に重く 5xx になりやすいため、失敗許容の補助）**
    ```
    [out:json][timeout:25];
    (
      nwr["name"~"道の駅"](around:R,la,ln);
      nwr["name"~"ＲＶパーク|RVパーク|オートキャンプ|キャンプ場|野営場|車中泊"](around:R,la,ln);
    );
    out center 120;
    ```
  - **この分離を崩さない**こと（1本に統合すると正規表現の負荷で全体が 504/503 になる実績あり）。
- 結果処理: `el.lat/lon` または `el.center` から座標取得。名前は `name` / `name:ja`。名前が無い要素は除外。
- **ホワイトリスト（品質の要）**: `classify()` で `道の駅 / SA・PA / 休憩所・PA / RVパーク / キャンプ場 / 車中泊スポット` のいずれかに該当したものだけ採用。該当しないものは捨てる。→ 無関係なノイズを構造的に排除。
- **ブラックリスト**: 名前が `/墓地|霊園|斎場|火葬|葬祭|葬儀|寺院|神社|仏閣|お墓|納骨/` に一致、またはタグが `landuse=cemetery` / `amenity=grave_yard|place_of_worship|funeral_hall` / `historic=*` / `access=private|no` のものは除外。
- **重複除外**: 既存（`SPOTS`＋`MY`）と 180m 以内は捨てる。名前＋座標でも重複除外。
- UI 表示: 採用件数と「対象外 N件は自動で除外」をトーストで出す。
- キャッシュ: 結果が 1 件以上のときのみ `carcamp_live_v1` に保存（0 件で前回分を潰さない）。24時間で失効。
  起動時に復元した場合はリスト先頭に「前回のライブ検索結果 N件を表示中（X分前）」の notice を出す。
- 失敗時（オフライン・全系統 5xx 等）はエラーメッセージ表示（アプリは落とさない）。
- **重要な限界**: OSM に「車中泊可否」を示すタグは存在しない。ゆえに本検索は **施設候補まで**しか出せず、可否は未確認である旨を UI で必ず明示する。上のホワイト/ブラックリストで質は大きく上がるが、実際に泊まれるかは最終的にストリートビュー等での目視・現地判断に委ねる（この割り切りが本アプリの前提）。

### 4.7 スポット追加（自分の）

- 「＋」FAB → 追加モード。地図に案内バナー表示。
- 場所指定: 地図タップ、または「現在地を使う」（基準点がタップ由来のときは「基準点を使う」表記）。**緑のピンはドラッグで微調整可**。
- フォーム項目: 名前（必須）・種類・エリア・設備トグル 4 種・静けさ 3 択・メモ。
- 保存 → `MY` に追加し localStorage へ。緑ピン＋「自分の」タグで一覧・地図に反映。
- 編集: 詳細の「編集」→ 同フォームで上書き。削除: 「削除」→ 確認後に除去。
- 入力途中で閉じようとしたときは破棄確認を出す。
- 保存キー: `carcamp_myspots_v1`。

### 4.8 データ入出力（バックアップ）

- リスト末尾「書き出し / 読み込み」→ データシート。
- 書き出し: `MY` を JSON でテキストエリアに出力＋**クリップボードへコピー**。**JSON ファイル保存**も可。
- 読み込み: JSON を貼り付け、または**ファイル選択** → `id` をキーにマージ（同 id は上書き、新規は追加）。
  欠損フィールドは既定値で補完（`q=2`、`cat='自分のスポット'` 等）。
- 用途: 端末変更・URL 変更時の引っ越し、バックアップ。

---

## 5. ビジュアル仕様

夜の水面のような深いティール地に、ヘッドランプ/常夜灯を思わせる琥珀色を差す。暗所で眩しくなく、OLED で省電力。

### カラートークン（CSS 変数）

| 変数 | 値 | 用途 |
|---|---|---|
| `--ink` | `#0B1519` | 背景（夜の水面） |
| `--surface` | `#12232A` | カード/シート |
| `--surface-2` | `#193138` | 一段上の面 |
| `--line` | `#24424B` | 罫線 |
| `--amber` | `#F4B740` | 主アクセント（灯り・最寄り） |
| `--moon` | `#7FD4C8` | 距離・水の手がかり |
| `--text` | `#EAF1EE` | 本文 |
| `--muted` | `#85A19B` | 補助テキスト |
| `--warn` | `#E8925A` | 要確認 |
| `--live` | `#9E8CFF` | OSM ライブ候補 |
| `--mine` | `#78D98E` | 自分の追加 |

### ピン色の対応

| 種別 | 色 |
|---|---|
| 道の駅 | `#9ec7ff` |
| RVパーク等（既定） | `--moon` |
| 無料系 | `#a9d98a` |
| 要確認（chk） | `--warn` |
| OSM ライブ（t=2） | `--live` |
| 自分の（t=3） | `--mine` |
| 最寄り（点滅） | `--amber` |
| 現在地（GPS） | 青 `#5ab7ff`（追尾中は精度円つき） |
| 基準点（タップ） | 琥珀の破線リング |
| 選択中 | 拡大＋白いリング（`.pin.sel`） |

### タイポグラフィ

- 日本語 UI: `Zen Kaku Gothic New`
- 数字（距離・単位）: `Space Grotesk`（等幅数字 `tabular-nums`）。距離を主役に見せる要。

---

## 6. 主要関数（実装の目印）

| 関数 | 役割 |
|---|---|
| `dist(a,b)` | ハバースインで km |
| `fmt(km)` | m / km 表示の整形（10km 以上は整数） |
| `bearingDeg(a,b)` / `bearingTxt(deg)` | 方位角と 8 方位 |
| `band(d)` | 距離帯見出しの判定 |
| `setBase(lat,lng,src)` | 基準点設定（'gps' / 'tap' / 'restored'） |
| `locateOnce()` / `startWatch()` / `stopWatch()` | 現在地取得と追尾 |
| `render()` | 一覧描画（近い順・距離帯・フィルタ・notice・フッター） |
| `syncMap(visible)` | フィルターと地図ピンの連動 |
| `allSpots()` | `SPOTS + MY + LIVE` を id 付きで結合 |
| `openSheet(id)` / `selectMarker(id)` / `markNearest(id)` | 詳細シートとピン強調 |
| `liveSearch()` / `overpassFetch(q)` / `classify(t,nm)` / `badTag(t)` / `drawLive()` | Overpass 検索・品質ルール・描画 |
| `startAdd()/startEdit()/placePin()/openForm()/saveForm()/deleteSpot()` | 追加・編集フロー |
| `redrawMy()` | 自分のピン再描画 |
| `openData()` | 書き出し/読み込み |
| `esc(s)` | HTML エスケープ（動的文字列は必ず通す） |

id 規約: `'s'+index`（SPOTS） / `'l'+index`（LIVE） / `'m'+spot.id`（MY）。

---

## 7. 非対象（やらないこと）

- ログイン・アカウント・ユーザー間共有。
- サーバー/DB。個人情報の収集。
- 車中泊可否の断定（可否は必ず「現地確認」に委ねる）。
- 広告・トラッキング。

---

## 8. 既知の制約・注意点（重要）

1. **localStorage はドメイン単位。** GitHub Pages で URL 固定済みのため通常は問題ない。URL を変える場合はバックアップ機能で引っ越す。
2. **地図タイルはオフライン非対応。** 圏外では一覧・距離は出るがタイルは表示されない。
3. **ライブ検索は要通信＆Overpass のレート制限あり。** 連打しない。失敗時はメッセージ表示。
   名前の正規表現検索は重く 5xx になりやすい → **タグ検索と分離し失敗許容**（4.6 参照）。この構成を崩さない。
4. **Google「地図で見る」は名前検索**のため、稀に同名の別地点に飛ぶ可能性。経路案内は座標なので正確。
5. **artifact/プレビュー環境では geolocation・localStorage・外部 fetch が制限される場合がある。** 実機ブラウザ／公開後に確認する。
   （Cowork のプレビューは外部 fetch を遮断する。Overpass 実通信は公開URLで検証すること）

---

## 9. 今後のロードマップ（拡張候補）

- **PWA 化**: `manifest.json` ＋ Service Worker。ホーム画面追加、アイコン、地図タイルのオフラインキャッシュ。
- **Google スプレッドシート連携**: スポットの共同編集・大量データ化（オフライン用にキャッシュ必須）。
- **データ外部化**: `data/spots.seed.json` を fetch。件数増に備える（SW キャッシュとセット）。
- **カテゴリ/種類フィルター**、**写真添付**（localStorage 容量に注意）、**釣り場との紐付け**。
- ライブ検索の対象・半径の設定 UI（例: 管理釣り場を含める、半径 10/15/20km）。

---

## 10. 受け入れ基準（動作チェックリスト）

- [ ] 起動時に現在地を取得し、近い順で一覧が出る。取得不可時は地図タップで基準点を置ける。
- [ ] ◎で追尾モードに入り、精度円が出て移動で並び替わる。再タップ／地図タップで停止する。
- [ ] 距離・方角（回転矢印つき）が各カードに正しく出て、距離帯見出しで区切られる。最寄りのピンが点滅する。
- [ ] 5 つのフィルターが AND で効き、地図のピンも連動する。t=2 はフィルター ON 時に隠れ、notice が出る。
- [ ] 詳細の「経路案内」で Google マップのナビが座標で開く。
- [ ] 詳細の「地図で見る」で Google マップの場所が開き、ストリートビューに入れる。
- [ ] ライブ検索で周辺の道の駅/RVパーク/キャンプ場/SA・PA が紫ピンで追加され、既存と重複しない。
      墓地等の対象外は除外され「対象外 N件は自動で除外」が出る。再起動でキャッシュが復元される。
- [ ] 「＋」→ 地図タップ/現在地/ドラッグで場所指定 → フォーム保存で緑ピンが増える。入力途中で閉じると破棄確認が出る。
- [ ] 追加スポット・フィルター・基準点・地図サイズが再読み込み後も復元される。
- [ ] 編集・削除ができる。
- [ ] 書き出し（コピー/ファイル）と読み込み（貼り付け/ファイル）で自分のスポットを移行できる。
- [ ] ボトムシートが下スワイプ・Escape・背景タップで閉じる。
- [ ] ダーク配色・日本語フォント・距離の数字表示が仕様どおり。

---

## 11. Claude Code への依頼例

- 「`index.html` を PWA 化して。`manifest.json` と Service Worker を追加し、地図タイルと同梱データをオフラインでも使えるようにして。単一デプロイのままにしたいので構成はできるだけシンプルに」
- 「ライブ検索の半径と対象種別を選べる設定 UI を追加して。既定は表示範囲から自動・道の駅/RVパーク/キャンプ場」
- 「`data/spots.seed.json` を読み込む方式に変更して。ただしオフライン表示を壊さないよう Service Worker でキャッシュして」
- 「自分の追加スポットに写真を1枚添付できるようにして（localStorage 容量に配慮し、リサイズして保存）」

---

### 付録: 初期データ

`data/spots.seed.json`（73 件, t=0:45 / t=1:28）。現行 `index.html` 冒頭の `const SPOTS` と同一内容。
スキーマは第 3 章のとおり。スポットを増減するときは**両方を同時に更新**する。
