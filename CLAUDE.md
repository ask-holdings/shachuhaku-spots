# CLAUDE.md — 車中泊さがし

このリポジトリは、釣りの後に**その場で車中泊スポットを探す**個人用 Web アプリです。
作業前に、まず本ファイルと `SPEC.md` を読んでください。

## これは何か（1行）
現在地から「近い順」で車中泊候補を表示する、**単一 HTML ファイル**の静的アプリ。

## ファイル構成
- `index.html` … アプリ本体（現行実装＝リファレンス）。ビルド不要。
- `SPEC.md` … 詳細仕様（設計思想・データモデル・機能・制約・受け入れ基準）。**作業前に必読**。
- `data/spots.seed.json` … 初期データ 73 件。`index.html` 冒頭の `const SPOTS` と同一内容。
- `README.md` … 起動・デプロイ手順（人間向け）。
- `index_5.html` … 初期デモ（比較用。git管理外・デプロイしない）。

## デプロイ（GitHub Pages）
- 公開URL: https://ask-holdings.github.io/shachuhaku-spots/
- リポジトリ: `ask-holdings/shachuhaku-spots`（main / root 配信）。**`git push` で自動反映**（約1分）。
- Windows側の作業フォルダは OneDrive 配下（このフォルダ）。認証は Git Credential Manager にキャッシュ済み。

## ローカルでの確認
ビルド不要。`file://` の直開きは GPS 等が動かないので不可。httpサーバ経由で開く。
```
# Codespaces / Python がある環境
python3 -m http.server 8000
```
```
# このPC（Python・Node なし）: .claude/serve.ps1 で http://localhost:8788
powershell -NoProfile -ExecutionPolicy Bypass -File .claude\serve.ps1
```

## 絶対に守る制約（設計を壊さないこと）
1. **単一ファイルを維持**：`index.html` 1 枚で完結。データは冒頭の `const SPOTS` にまとめる。
   分割する場合は必ず「オフラインでも一覧が出る」を守る策（Service Worker キャッシュ）とセットで。
2. **4 つの設計思想を守る**（詳細は SPEC.md 第 1 章）
   - 距離が主役（開いたら現在地から近い順）
   - 夜向けの情報設計（一覧は即決材料だけ／詳細で夜の安心材料とメモ）
   - 正直さ（「要確認」「未確認候補」を必ず区別して表示）
   - 軽いオフライン（同梱データは通信なしでも表示できる）
3. **車中泊可否を断定しない**：OSM に可否データは存在しない。ライブ検索の結果は
   「未確認の候補」として出し、可否判断はユーザー（ストリートビュー・現地）に委ねる。
4. **ダーク配色とデザイントークンを踏襲**（SPEC.md 第 5 章）。距離は Space Grotesk の等幅数字で主役に。

## よくある落とし穴（先に知っておくこと）
- **localStorage はドメイン単位**。GitHub Pages で URL 固定済みなので通常は問題ないが、
  URL を変える場合はバックアップ（書き出し/読み込み）で移行する。
- **地図タイルはオフライン非対応**（同梱データの一覧・距離は表示できる）。
- **ライブ検索は Overpass API**（要通信・レート制限あり）。連打しない。失敗時はメッセージ表示で落とさない。
  実装はタグ検索（必須）と名前の正規表現検索（重いので**失敗許容の補助**）の2本に分離し、
  エンドポイントは3系統フォールバック。**この分離構成を崩さない**（名前検索を必須に戻すと5xxで全滅する）。
- **プレビュー/サンドボックス環境では GPS・localStorage・外部 fetch が制限される**ことがある。実機/公開で確認する。
  （Cowork のプレビューは外部 fetch を遮断するため、Overpass 実通信の検証は公開URLか実機で行う）

## 変更の進め方
- まず `SPEC.md` の該当章を読む。完了時は **受け入れ基準（SPEC.md 第 10 章）** を満たすこと。
- スポットを増やすときは `index.html` の `const SPOTS` と `data/spots.seed.json` の**両方**を更新し、件数を一致させる。
- ライブ検索の品質ルール（ホワイトリスト＋墓地などの除外）は SPEC.md 4.6 にある。緩めるときは注意。
- 拡張候補は SPEC.md 第 9 章、そのまま使える依頼例は第 11 章にある。
